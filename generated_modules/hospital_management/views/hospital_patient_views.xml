<odoo>
    <data>
        <!-- Tree View for Patient Records -->
        <record id="hospital_patient_tree_view" model="ir.ui.view">
            <field name="name">hospital.patient.tree</field>
            <field name="model">hospital.patient</field>
            <field name="arch" type="xml">
                <tree>
                    <field name="name"/>
                    <field name="age"/>
                    <field name="gender"/>
                    <field name="doctor_id"/>
                </tree>
            </field>
        </record>

        <!-- Form View for Patient Records -->
        <record id="hospital_patient_form_view" model="ir.ui.view">
            <field name="name">hospital.patient.form</field>
            <field name="model">hospital.patient</field>
            <field name="arch" type="xml">
                <form>
                    <sheet>
                        <group>
                            <field name="name"/>
                            <field name="age"/>
                            <field name="gender"/>
                            <field name="doctor_id"/>
                            <field name="appointment_ids"/>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- Search View for Patient Records -->
        <record id="hospital_patient_search_view" model="ir.ui.view">
            <field name="name">hospital.patient.search</field>
            <field name="model">hospital.patient</field>
            <field name="arch" type="xml">
                <search>
                    <field name="name"/>
                    <field name="doctor_id"/>
                    <separator/>
                    <filter name="male_patients" string="Male Patients" domain="[('gender', '=', 'male')]"/>
                    <group expand="0" string="Group By">
                        <filter name="gender" string="Gender" context="{\'group_by\': \'gender\'}"/>
                    </group>
                </search>
            </field>
        </record>
    </data>
</odoo>